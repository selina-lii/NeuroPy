
--- 2026-04-08T11:06:01.718511 ---
0.test
1.test
2.test3

--- 2026-04-24T22:48:01.406119 ---
Be precise and stringent on token use while ensuring 100% accuracy.
Always consider ***SIMPLIFYING*** and ***REUSING*** existing code before adding duplicate functions, but check for absolute accuracy and reliability.
Ask yourself: Are there any duplicate code logic that can be simplified?

1. CCG normalization: when normalize by Same scale (pair), high-res CCG should be processed separately  using its own maximum instead of using the same y scale as low-res!
2. CCG normalization: time span doesn't work for custom CCG from Time Slider now. Save a metadata field 'total_time' with each custom CCG; save which theme and brain states were selected to compute the custom CCG. (Reminder to self: this would invalidate all previous custom CCGs, since they don't have total time. All previous custom CCGs should be cleaned.)
3. promptspace hypotheses: create different tabs of hypotheses. the data must be backed up with the same logic as ccg pair selections. to the right of add hypotheses,+, add save and load buttons (use the save load icons as in time slider of ccgviewer), with the same logic but copy the code here - do not reference ccgviewer as those two should be dissociated. above add hypotheses create a text box that's the name of the current hypotheses canvas. the default name is new canvas x if no name is specified, x being the next integer after all previously default-named canvases.it is very very very important that you keep hypotheses data secure becaus ethose are very complicated data that cannot be recovered by memory once lost, that's gonna be like the equivalent of google docs deleting all your docs.. very bad. never delete any hypotheses data saved, just move them to a trash folder
