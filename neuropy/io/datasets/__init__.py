"""Per-dataset conventions: field maps, session naming, theme and epoch labels.

FIELDS holds the source names still to be parsed onto NeuroPy's schema — a dataset
that already arrives NeuroPy-named (one read through ProcessData) omits it. Every
other module attribute is already in NeuroPy's vocabulary and needs no binding.
"""
